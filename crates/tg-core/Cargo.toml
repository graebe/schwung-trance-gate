[package]
name = "tg-core"
version = "1.0.0"
edition = "2021"
authors = ["Torben Gräber"]
license = "MIT"
description = "Trance Gate DSP engine: a tempo-locked step gate with a per-step ADSR"

[lib]
crate-type = ["rlib"]
