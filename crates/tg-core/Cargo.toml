[package]
name = "tg-core"
version = "1.0.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Trance Gate DSP engine: a tempo-locked step gate with a per-step ADSR"

[lib]
crate-type = ["rlib"]
